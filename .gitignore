# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build outputs
dist
build

# Scripts utilitários e arquivos temporários da raiz (não commitar)
auth_check.mjs
check_profiles.mjs
test_fal.mjs
*_check.mjs
tmp_app/
git_log.txt

# Relatório de segurança (contém detalhes de exploits — manter fora do git)
RELATORIO_SEGURANCA.md
SECURITY_CHECKLIST_STATUS.md

# Relatórios pré-produção (segurança, testes, prontidão — detalhes sensíveis, fora do git)
_relatorios-preprod/

# Auditoria de segurança (PDF + gerador embutem detalhes de exploits — fora do git)
docs/security-audit/

# Notebooks de teste/exploração (podem conter segredos — não commitar)
*.ipynb

# Diretório local de configuração do agente (Claude Code)
.claude/


# Estado local do Supabase CLI (nunca versionar: aponta para um projeto real)
supabase/.temp/
supabase/.branches/
.env
.env.local
